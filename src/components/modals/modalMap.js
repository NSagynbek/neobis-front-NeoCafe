import { 
  AttentionModal, 
  ExampleModal,
  NotificationCenter,
} from "./index";

const modalsMap = {
    attention: AttentionModal,
    example: ExampleModal,
    notification: NotificationCenter,
  };
  
  export default modalsMap;